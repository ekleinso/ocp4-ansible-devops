apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: {{ etcd_backup_pvc_claim }}
  namespace: {{ etcd_backup_namespace }}
spec:
  accessModes:
  - ReadWriteMany
  resources:
    requests:
      storage: {{ etcd_backup_volume_size }}
  storageClassName: "{{ etcd_backup_storageclass }}"
