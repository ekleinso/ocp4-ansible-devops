apiVersion: rbac.authorization.k8s.io/v1
kind: Role
metadata:
  labels:
    app: "{{ mvie_namespace }}"
  name: mvie-scc-anyuid
  namespace: "{{ mvie_namespace }}"
rules:
- apiGroups:
  - security.openshift.io
  resourceNames:
  - anyuid
  resources:
  - securitycontextconstraints
  verbs:
  - use
